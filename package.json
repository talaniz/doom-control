{
  "dependencies": {
    "dompurify": "3.4.15",
    "marked": "18.0.13",
    "ws": "8.21.3"
  },
  "name": "doom-control-room",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Private LAN and WireGuard browser interface for the Pi Codex app server",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check auth.mjs && node --check scripts/provision-users.mjs && node --check dist/app.js && node --check uploads.mjs && node --check dist/links.js && node --check dist/picker.js && node --check projects.mjs && node --check dist/projects.js",
    "test": "node --test test/*.test.mjs",
    "users:provision": "node scripts/provision-users.mjs"
  },
  "devDependencies": {
    "jsdom": "30.1.0"
  }
}
